import React from "react";

export default function Container() {
  return (
    <div
      style={{
        width: "75%",
        backgroundColor: "gray",
        border: "1px",
        margin: "à auto",
      }}
    ></div>
  );
}
